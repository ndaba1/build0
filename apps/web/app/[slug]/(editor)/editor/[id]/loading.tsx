"use client";

import { Loader } from "@/components/loader";

export default function Loading() {
  return (
    <Loader message="Please wait while we load an absurd amount of JavaScript into your browser..." />
  );
}
